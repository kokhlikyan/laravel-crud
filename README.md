# Cretrix Task Basic auth and CRUD

# Runing process

#### you need to change the database connection data and after that run these commands

```bash
php artisan migrate  
php artisan db:seed 
php artisan serve  ßß
```

